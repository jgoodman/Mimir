% layout 'default';
% title 'Mimir - Node';

<h2><%= $title %></h2>

% foreach my $leaf (@$leaves) {
    <p><a href="/leaf/<%= $leaf->{'leaf_id'} %>"><%= $leaf->{'content'} %></a></p>
% }

%= include 'add-leaf-model', node_id => $node_id

